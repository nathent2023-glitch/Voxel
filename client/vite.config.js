import { defineConfig } from "vite";

export default defineConfig({
  // GitHub Pages serves your site at /<repo-name>/
  // Your repo is "Voxel", so the base must be "/Voxel/"
  base: "/Voxel/",
});
